const toDos = [];

const toDoFactory = (title, description, notes, dueDate, priority, projectName) =>
  //   const proto = {};

  ({
    title,
    description,
    notes,
    dueDate,
    priority,
    projectName,
  });
export { toDoFactory };

const asd = {
  description: 'asd',
  dueDate: '2023-05-18',
  notes: 'asd',
  priority: 'low',
  projectName: '',
  title: 'asd',
};
